import 'package:flutter/material.dart';
class LendenScreen extends StatelessWidget {
  const LendenScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Recieve Screen"),
      ),
    );
  }
}
